# Vital Guardian - Required Dependencies
# Installation: pip install -r requirements.txt
#
# Scope: production runtime only (scripts/demo/demo_server.py entrypoint).
# Packages only used in notebooks, evaluation scripts, or training are excluded.

# Core ML/DL
torch>=2.0.0
torchvision>=0.15.0
tensorflow>=2.14.0
opencv-python>=4.8.0
numpy                 # MUST BE < 2.0 for tf-models-official compatibility
pillow>=10.0.0        # Hard dependency of torchvision and ultralytics

# Computer Vision
ultralytics>=8.0.0          # YOLO person detector
openvino>=2024.0.0          # OpenVINO runtime (fallback CPU/iGPU backend for YOLO)
mediapipe>=0.10.0           # Pose estimation (PoseAnalyzer)
tf_keras>=2.14.0            # MoViNet backbone requirement
tf-models-official==2.20.0  # Provides 'official.projects.movinet' — required by movinet_loader.py

# Audio Processing
librosa>=0.10.0             # Audio loading (demo_server.py audio injection)
soundfile>=0.12.0           # Audio I/O (librosa dependency)
tensorflow-hub>=0.12.0      # YAMNet distress model
faster-whisper>=1.0.0       # Offline Speech-to-Text (keyword spotter)
silero-vad                  # Voice Activity Detection (privacy shield)
pyaudio                     # Microphone capture (stub-safe when MIC_ENABLED=false)

# AI / LLM — Gemini (official SDK)
google-genai>=1.51.0         # Google Gemini API (requires 1.51+ for Gemini 3)
pydantic>=2.0.0              # Structured output validation for IncidentReport
python-dotenv>=1.0.0         # Load secrets from .env file

# Web Framework & Persistence
fastapi>=0.110.0            # Main API server
uvicorn>=0.29.0             # ASGI server
httpx>=0.27.0               # Async HTTP client (Kaggle endpoint + health-check)
sqlalchemy>=2.0.0           # ORM for incident/patient/audit DB
psycopg2-binary>=2.9.0      # PostgreSQL driver for SQLAlchemy

# Utilities
pyyaml>=6.0                 # config/config.yaml loading
